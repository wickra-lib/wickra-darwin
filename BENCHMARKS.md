# Benchmarks

Numbers land here once the Criterion suite (`crates/darwin-bench`) is wired up in
P-DAR-5. The headline figure is **backtests per second** — the rate at which the
evolution loop can score candidate specs through the `wickra-backtest` engine.

Reproduce with:

```bash
cargo bench -p darwin-bench
```

_Placeholder — replaced with measured figures during P-DAR-5._
