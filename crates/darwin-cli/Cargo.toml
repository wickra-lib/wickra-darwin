[package]
name = "wickra-darwin"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "The wickra-darwin reference CLI: evolve strategies from candle data."

[[bin]]
name = "wickra-darwin"
path = "src/main.rs"

[package.metadata.docs.rs]
# Document every feature so an optional surface is not missing from the
# reference. docs.rs builds on nightly and sets the `docsrs` cfg, which is what
# makes the feature badges appear.
all-features = true

[dependencies]
wickra-darwin-core = { workspace = true }
wickra-backtest = { workspace = true }
serde_json = { workspace = true }
clap = { workspace = true }

[lints]
workspace = true
