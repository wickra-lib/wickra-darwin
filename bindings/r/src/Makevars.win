# On Windows the C ABI DLL is linked directly (mingw resolves the import stubs
# from wickra_darwin.dll); the loader finds it via PATH at run time.
PKG_CPPFLAGS = -I$(WKDARWIN_INC)
PKG_LIBS = -L$(WKDARWIN_LIB) -l:wickra_darwin.dll
